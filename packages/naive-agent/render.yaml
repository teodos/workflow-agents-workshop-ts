# naive-agent. One web service + Postgres.
# Builds from the repo root so the npm workspaces resolve.

projects:
  - name: teodos-agents-workshop-naive
    environments:
      - name: production
        databases:
          - name: teodos-naive-agent-db
            plan: basic-256mb
            region: oregon
            postgresMajorVersion: "18"

        services:
          - type: web
            name: teodos-naive-agent
            runtime: node
            region: oregon
            plan: starter
            buildCommand: npm ci
            startCommand: npm run start --workspace @workshop/naive-agent
            healthCheckPath: /healthz
            envVars:
              - key: DATABASE_URL
                fromDatabase:
                  name: teodos-naive-agent-db
                  property: connectionString
              - key: NODE_ENV
                value: production
